主要完成的是主界面和背景页面的切换，以及在动态游戏界面加入了种植植物和退出的按钮。
  新加入了一些图片和音频资源
  新加入文件plants，起炮塔的作用。cpp有两个函数，一个是构造函数，
另一个draw可以在指定位置画出所需要的植物图片（图片文件中存了4个，用于升级所需）
加入mainwindow文件，首先setfixedsize确定主界面的大小，mybutton 建立新的按钮，与
之后的两个connect连接后用于切换界面。析构函数及时释放内存，draw函数画出次界面。
加入新文件mybutton，在游戏界面创建按钮，用set函数确定位置大小，用于完成页面切换
以及植物种植
  新加入了mywindow文件，用于在次界面做修改。其中有一个chooseback信号没用于界面
的切换用mybutton加入了5个按钮，对于三个数字按钮（关卡选择）对应有两个set
函数确定按钮的大小，move函数确定他的位置。这5个按钮中一个与之前主界面用click方法
connect实现主次界面的切换，剩下的三个按钮实现关卡的选择，通过第一关才可继续后面关卡
最后一个按钮控制进入游戏动态界面，connect中用到了mw类建立新的页面，也就是动态的
游戏主界面。setFixedSize确定当前界面的大小。setplants函数可以控制位置和目标图片
创建了plants list用于管理，加入update进行刷新，使得每画一次可以刷新显示炮塔。
paintevent执行绘画功能。
  pushbutton文件重写mousePressEvent，mouseMoveEvent两个函数用于满足鼠标拖动
信号实现拖拽植物（炮塔）
  world文件：修改了坐标图片
  icon：修改了GRID_SIZE
  main cpp 修改了调出主窗口的方法，不过出现了问题，在第三个窗口设置的控制种植植物的
按钮目前只涉及了一个作为实验，但过程中出现了用于控制种植植物的button，但大小位置
和设置的不一样并且点击也没有反应，位于铲子下方的back按钮也如是，应该没有和click信号
成功匹配，原因未知。
